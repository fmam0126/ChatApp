✅ Add Authentication — JWT symmetric HMAC-SHA256, /auth/login endpoint, token issued per username
✅ Duplicate username prevention — per-session via ConnectedUsersService
✅ Message persistence — SQLite database, messages saved in ChatHub.SendMessage
✅ Chat history — GET /ChatMessages with ?count= param, authorized, ordered by date
✅ Input sanitization — message length limit, control character stripping, username validation
✅ System messages — join/leave notifications broadcast to all clients
✅ User connection tracking — ConnectedUsersService with ConcurrentDictionary
✅ SignalR security — token from query string for WebSocket connections